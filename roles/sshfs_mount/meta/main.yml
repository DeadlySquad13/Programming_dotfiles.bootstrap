---
galaxy_info:
  author: your_name
  description: Configure SSHFS mount using systemd user units
  license: MIT
  min_ansible_version: "2.9"
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: EL
      versions:
        - all
  galaxy_tags:
    - sshfs
    - mount
    - systemd
    - fuse

dependencies: 
  - sshfs
